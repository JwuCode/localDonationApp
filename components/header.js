import {
    Text, 
    Image, 
    View, 
    ScrollView, 
    SafeAreaView, 
    Button, 
    StyleSheet
    } from 'react-native';

export default Header = () =>{
    return (<Text style={{color:"black"}}>Generocity</Text>)
}